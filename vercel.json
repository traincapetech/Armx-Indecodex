{
  "buildCommand": "cd client && npm install --legacy-peer-deps && node scripts/prepare-build.js && node scripts/generate-sitemap.js && CI=false npx craco build",
  "outputDirectory": "client/build",
  "installCommand": "echo 'Skipping root install'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
